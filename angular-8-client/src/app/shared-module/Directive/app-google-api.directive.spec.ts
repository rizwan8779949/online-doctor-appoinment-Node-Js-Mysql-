import { AppGoogleApiDirective } from './app-google-api.directive';

describe('AppGoogleApiDirective', () => {
  it('should create an instance', () => {
    const directive = new AppGoogleApiDirective();
    expect(directive).toBeTruthy();
  });
});
